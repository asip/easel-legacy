google:
  client_id: <%= ENV.fetch('GOOGLE_CLIENT_ID') { '' } %>
  client_secret: <%= ENV.fetch('GOOGLE_CLIENT_SECRET') { '' } %>
front:
  api:
    origin: /api/front/v1
external:
  google_callback_url: /oauth/callback?provider=google