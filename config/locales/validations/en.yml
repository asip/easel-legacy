en:
  validations:
    message:
      user:
        image:
          max_size: ' are limited to 5MB'
          mime_type: ' are JPEG/PNG only'
          required: ' required.'
      frame:
        file:
          max_size: ' are limited to 5MB'
          mime_type: ' are JPEG/PNG only'
          required: ' required.'
        tags:
          array_length: ' are limited to 5'
          length: ' are limited to 10 characters'