<% if @photoswipe %>
  <%= render Lightbox::PhotoSwipe::Component.new(
    img_url: @img_url,
    link_url: @link_url) %>
<% else %>
  <%= render Lightbox::GLightbox::Component.new(
    img_url: @img_url,
    link_url: @link_url,
    small: @small) %>
<% end %>
