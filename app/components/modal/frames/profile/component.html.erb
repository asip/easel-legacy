<dialog id="frame_profile_modal" class="modal">
  <div class="modal-box">
    <%= render Frames::Profile::Component.new(frame: @frame) %>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>close</button>
  </form>
</dialog>
