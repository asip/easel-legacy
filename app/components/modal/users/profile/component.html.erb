<dialog id="user_profile_modal" class="modal">
  <div class="modal-box divide-y divide-gray-200">
    <%= render Users::Profile::Component.new(user: @user) %>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>close</button>
  </form>
</dialog>
