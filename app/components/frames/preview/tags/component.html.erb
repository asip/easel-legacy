<% if @list %>
  <% if @tag_paths.size > 0 %>
    <div>
      <% @tag_paths.each do |tag, tag_path| %>
        <%= link_to tag_path, "data-turbo": false do %><div class="badge rounded-full"><%= tag %></div><% end %>
      <% end %>
    </div>
  <% end %>
<% else %>
  <% if @tag_paths.size > 0 %>
    <div class="flex justify-center mb-2">
      <% @tag_paths.each do |tag, tag_path| %>
        <%= link_to tag_path, "data-turbo": false do %><div class="badge rounded-full"><%= tag %></div><% end %>
      <% end %>
    </div>
  <% end %>
<% end %>
