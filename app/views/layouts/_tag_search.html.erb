<%= form_with url: root_path, local: true, method: :get, class: 'd-flex' do |form| %>
  <%= form.text_field :q, placeholder: 'タグ or 名前', class: 'form-control me-sm-2', value: @word %>
  <%= form.submit '検索', class: 'btn btn-outline-success me-2 me-sm-0' %>
<% end %>
