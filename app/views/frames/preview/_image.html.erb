        <% if frame.file.present? %>
        <% if original %>
        <div class="d-flex justify-content-sm-center">
          <% if spotlight.present? && spotlight == true %>
          <%= link_to image_tag(frame.file_url(:three), class: 'mx-auto'), frame.file_url, class: 'mx-auto spotlight' %>
          <% else %>
          <%= link_to image_tag(frame.file_url(:three), class: 'mx-auto'), frame.file_url, class: 'mx-auto', "data-frame-target": "lm" %>
          <% end %>
        </div>
        <% else %>
        <div class="d-flex justify-content-sm-center">
          <%= image_tag(frame.file_url(:three), class: 'mx-auto') %>
        </div>
        <% end %>
        <br>
        <% end %>